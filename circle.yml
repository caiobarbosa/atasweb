machine:
  ruby:
    version: 2.3.0
  services:
    - docker

dependencies:
  post:
    - docker build -t $AWS_ACCOUNT_ID.dkr.ecr.us-west-2.amazonaws.com/salvus/atas:2 .

deployment:
  prod:
    branch: master
    commands:
      - ./deploy.sh
